{base}

Diff:
```
{diff_content}
```
{merged_section}{history_block}
Your verdict MUST be on its own line, exactly one of:
VERDICT: APPROVED
VERDICT: NEEDS_CHANGES

Pick APPROVED if the changes are correct and complete. Pick NEEDS_CHANGES if there are specific issues to fix.
If you pick NEEDS_CHANGES, list every issue as a numbered item with the file path and what needs to change.
Pacing: if this diff duplicates, re-litigates, or churns work already listed under "Recently merged work on main", pick NEEDS_CHANGES and say which merged subject it overlaps — do not approve redundant or repeated work.
