{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/nbardy/oompa_loompas/schemas/cycle.schema.json",
  "title": "OompaCycle",
  "description": "Worker cycle event. One complete work unit. File: runs/{swarm-id}/cycles/{worker-id}-c{N}.json",
  "type": "object",
  "required": ["worker-id", "cycle", "outcome", "timestamp", "duration-ms", "recycled-tasks", "review-rounds"],
  "properties": {
    "worker-id": { "type": "string" },
    "cycle": { "type": "integer", "minimum": 1 },
    "attempt": {
      "type": "integer",
      "minimum": 1,
      "description": "1-based resume attempt within this cycle (counts against max-resumes)"
    },
    "outcome": {
      "type": "string",
      "enum": ["merged", "rejected", "error", "done", "executor-done", "no-changes", "no-claim", "working", "claimed", "sync-failed", "merge-failed", "interrupted", "stuck", "needs-followup"]
    },
    "timestamp": { "type": "string", "format": "date-time" },
    "duration-ms": { "type": "integer", "minimum": 0 },
    "claimed-task-ids": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Task IDs claimed via CLAIM signal during this cycle"
    },
    "recycled-tasks": {
      "type": "array",
      "items": { "type": "string" }
    },
    "error-snippet": { "type": ["string", "null"] },
    "review-rounds": { "type": "integer", "minimum": 0 },
    "session-id": {
      "type": ["string", "null"],
      "description": "agent-cli session id — salvage's resume handle for this cycle"
    },
    "worktree-path": {
      "type": ["string", "null"],
      "description": "Filesystem path of the worktree this cycle ran in"
    },
    "signals": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Cumulative per-attempt signal chain (e.g. claim:..., working, merge)"
    },
    "merge-sha": {
      "type": ["string", "null"],
      "description": "Short commit SHA when this cycle merged to main, else null"
    },
    "timing-ms": {
      "type": "object",
      "properties": {
        "implementation-rounds-ms": {
          "type": "array",
          "items": { "type": "integer", "minimum": 0 }
        },
        "reviewer-response-ms": {
          "type": "array",
          "items": { "type": "integer", "minimum": 0 }
        },
        "review-fixes-ms": {
          "type": "array",
          "items": { "type": "integer", "minimum": 0 }
        },
        "optional-review-ms": {
          "type": "array",
          "items": { "type": "integer", "minimum": 0 }
        },
        "harness-ms": {
          "type": "integer",
          "minimum": 0
        },
        "other-ms": {
          "type": "integer",
          "minimum": 0
        },
        "llm-calls": {
          "type": "array",
          "items": {
            "type": "object",
            "required": ["name", "section", "duration-ms"],
            "properties": {
              "name": { "type": "string" },
              "section": { "type": "string" },
              "duration-ms": { "type": "integer", "minimum": 0 },
              "percent": { "type": "number", "minimum": 0, "maximum": 100 }
            },
            "additionalProperties": false
          }
        }
      },
      "additionalProperties": false
    }
  },
  "additionalProperties": false
}
